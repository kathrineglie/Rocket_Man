package inf112.rocketman.model;


import inf112.rocketman.grid.Grid;

public class GameBoard extends Grid {

    public GameBoard(int rows, int cols, Character defaultValue) {
        super(rows, cols, defaultValue);
    }
}
    
